apiVersion: user.taikun.jet.crossplane.io/v1alpha1
kind: User
metadata:
    name: test-all-user
spec:
  forProvider:
    userName: "test-all-user"
    email: "user@delete.com"
    role: "Partner"
    organizationIdRef:
        name: "test-all-organization-attac-attach"
  providerConfigRef:
    name: default
